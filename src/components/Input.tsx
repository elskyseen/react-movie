const Input = () => {
  return (
    <input
      type="email"
      className="bg-transparent border-b border-primary outline-none placeholder:text-white/40 text-white px-1 py-2 text-xs"
      placeholder="johndoe@example.com"
    />
  );
};

export default Input;
